model List_EBook_Counts{
    id Int @id @default(autoincrement())
    titles String?
    volumes Int?
    year Int?
    updatedat DateTime
    chapters Int?
    ishidden Boolean?

    listebook Int? @unique
    List_EBook List_EBook? @relation(fields: [listebook], references: [id])
}